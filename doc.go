/*Package partialmarkup takes a given string (document) that can contain at
markup tags (HTML Style) such as <div></div>, and convert each element to a
readable tree that can be used by an application.
*/
package partialmarkup
